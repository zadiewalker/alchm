# NUCLEAR NPM-ONLY CONFIGURATION for Firebase Studio
# This forces buildpack to use npm exclusively

# Disable pnpm completely
enable-pnp=false
package-manager-strict=true
engine-strict=true

# Firebase Studio build optimizations
audit=false
fund=false
progress=false
loglevel=error

# Force npm registry usage
registry=https://registry.npmjs.org/