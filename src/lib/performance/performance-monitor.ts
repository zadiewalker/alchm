/**
 * ALCHM Performance Monitoring System
 * Crisis-Critical: Automated rollback triggers for performance regressions
 * Real-Time: Sub-second detection of performance issues affecting crisis users
 */

interface PerformanceMetric {
  name: string;
  value: number;
  timestamp: number;
  url: string;
  userAgent: string;
  isCrisisUser?: boolean;
  sessionId?: string;
}

interface PerformanceBudget {
  metric: string;
  budget: number;
  threshold: number; // Percentage over budget that triggers alert
  crisisBudget?: number; // Stricter budget for crisis users
}

interface PerformanceAlert {
  id: string;
  type: 'budget_exceeded' | 'regression' | 'crisis_performance' | 'memory_leak';
  severity: 'warning' | 'critical' | 'emergency';
  message: string;
  metrics: PerformanceMetric[];
  timestamp: number;
  rollbackTriggered?: boolean;
}

export class PerformanceMonitor {
  private metrics: PerformanceMetric[] = [];
  private alerts: PerformanceAlert[] = [];
  private observer: PerformanceObserver | null = null;
  private isMonitoring = false;
  private rollbackCallbacks: Array<() => Promise<void>> = [];
  
  // Performance budgets (in milliseconds)
  private budgets: PerformanceBudget[] = [
    { metric: 'LCP', budget: 2500, threshold: 10, crisisBudget: 1200 },
    { metric: 'FID', budget: 100, threshold: 20, crisisBudget: 50 },
    { metric: 'CLS', budget: 0.1, threshold: 50, crisisBudget: 0.05 },
    { metric: 'TTFB', budget: 800, threshold: 25, crisisBudget: 400 },
    { metric: 'FCP', budget: 1800, threshold: 15, crisisBudget: 900 }\n  ];\n  \n  constructor() {\n    this.initializeObserver();\n    this.startMemoryMonitoring();\n    this.registerCleanupListeners();\n  }\n  \n  /**\n   * Start performance monitoring\n   */\n  startMonitoring(): void {\n    if (this.isMonitoring || typeof window === 'undefined') return;\n    \n    this.isMonitoring = true;\n    \n    // Monitor Core Web Vitals\n    this.monitorWebVitals();\n    \n    // Monitor custom metrics\n    this.monitorCustomMetrics();\n    \n    // Monitor navigation timing\n    this.monitorNavigationTiming();\n    \n    console.log('[Performance] Monitoring started with crisis-optimized budgets');\n  }\n  \n  /**\n   * Stop performance monitoring\n   */\n  stopMonitoring(): void {\n    this.isMonitoring = false;\n    \n    if (this.observer) {\n      this.observer.disconnect();\n      this.observer = null;\n    }\n    \n    console.log('[Performance] Monitoring stopped');\n  }\n  \n  /**\n   * Register rollback callback for performance issues\n   */\n  registerRollbackCallback(callback: () => Promise<void>): void {\n    this.rollbackCallbacks.push(callback);\n  }\n  \n  /**\n   * Monitor Core Web Vitals with crisis optimization\n   */\n  private monitorWebVitals(): void {\n    if (!('PerformanceObserver' in window)) return;\n    \n    try {\n      // Monitor LCP (Largest Contentful Paint)\n      new PerformanceObserver((list) => {\n        for (const entry of list.getEntries()) {\n          this.recordMetric({\n            name: 'LCP',\n            value: entry.startTime,\n            timestamp: Date.now(),\n            url: window.location.href,\n            userAgent: navigator.userAgent,\n            isCrisisUser: this.isCrisisUser()\n          });\n        }\n      }).observe({ entryTypes: ['largest-contentful-paint'] });\n      \n      // Monitor FID (First Input Delay)\n      new PerformanceObserver((list) => {\n        for (const entry of list.getEntries()) {\n          this.recordMetric({\n            name: 'FID',\n            value: (entry as any).processingStart - entry.startTime,\n            timestamp: Date.now(),\n            url: window.location.href,\n            userAgent: navigator.userAgent,\n            isCrisisUser: this.isCrisisUser()\n          });\n        }\n      }).observe({ entryTypes: ['first-input'] });\n      \n      // Monitor CLS (Cumulative Layout Shift)\n      new PerformanceObserver((list) => {\n        let clsValue = 0;\n        for (const entry of list.getEntries()) {\n          if (!(entry as any).hadRecentInput) {\n            clsValue += (entry as any).value;\n          }\n        }\n        \n        this.recordMetric({\n          name: 'CLS',\n          value: clsValue,\n          timestamp: Date.now(),\n          url: window.location.href,\n          userAgent: navigator.userAgent,\n          isCrisisUser: this.isCrisisUser()\n        });\n      }).observe({ entryTypes: ['layout-shift'] });\n      \n    } catch (error) {\n      console.error('[Performance] Failed to initialize Web Vitals monitoring:', error);\n    }\n  }\n  \n  /**\n   * Monitor custom performance metrics\n   */\n  private monitorCustomMetrics(): void {\n    // Monitor component render times\n    this.measureComponentPerformance();\n    \n    // Monitor API response times\n    this.monitorAPIPerformance();\n    \n    // Monitor bundle loading times\n    this.monitorResourceLoading();\n  }\n  \n  /**\n   * Monitor navigation timing\n   */\n  private monitorNavigationTiming(): void {\n    window.addEventListener('load', () => {\n      setTimeout(() => {\n        const navigation = performance.getEntriesByType('navigation')[0] as PerformanceNavigationTiming;\n        \n        if (navigation) {\n          this.recordMetric({\n            name: 'TTFB',\n            value: navigation.responseStart - navigation.requestStart,\n            timestamp: Date.now(),\n            url: window.location.href,\n            userAgent: navigator.userAgent,\n            isCrisisUser: this.isCrisisUser()\n          });\n          \n          this.recordMetric({\n            name: 'DOMContentLoaded',\n            value: navigation.domContentLoadedEventEnd - navigation.navigationStart,\n            timestamp: Date.now(),\n            url: window.location.href,\n            userAgent: navigator.userAgent,\n            isCrisisUser: this.isCrisisUser()\n          });\n        }\n      }, 0);\n    });\n  }\n  \n  /**\n   * Measure component render performance\n   */\n  private measureComponentPerformance(): void {\n    // Hook into React DevTools if available\n    if (typeof window !== 'undefined' && (window as any).__REACT_DEVTOOLS_GLOBAL_HOOK__) {\n      const hook = (window as any).__REACT_DEVTOOLS_GLOBAL_HOOK__;\n      \n      hook.onCommitFiberRoot = (id: any, root: any, priorityLevel: any) => {\n        const startTime = performance.now();\n        \n        // Measure render time\n        setTimeout(() => {\n          const renderTime = performance.now() - startTime;\n          \n          if (renderTime > 16) { // Longer than one frame\n            this.recordMetric({\n              name: 'ComponentRender',\n              value: renderTime,\n              timestamp: Date.now(),\n              url: window.location.href,\n              userAgent: navigator.userAgent,\n              isCrisisUser: this.isCrisisUser()\n            });\n          }\n        }, 0);\n      };\n    }\n  }\n  \n  /**\n   * Monitor API performance\n   */\n  private monitorAPIPerformance(): void {\n    const originalFetch = window.fetch;\n    \n    window.fetch = async (...args) => {\n      const startTime = performance.now();\n      const url = args[0]?.toString() || 'unknown';\n      \n      try {\n        const response = await originalFetch(...args);\n        const endTime = performance.now();\n        \n        this.recordMetric({\n          name: 'API_Response',\n          value: endTime - startTime,\n          timestamp: Date.now(),\n          url: url,\n          userAgent: navigator.userAgent,\n          isCrisisUser: this.isCrisisUser()\n        });\n        \n        return response;\n      } catch (error) {\n        const endTime = performance.now();\n        \n        this.recordMetric({\n          name: 'API_Error',\n          value: endTime - startTime,\n          timestamp: Date.now(),\n          url: url,\n          userAgent: navigator.userAgent,\n          isCrisisUser: this.isCrisisUser()\n        });\n        \n        throw error;\n      }\n    };\n  }\n  \n  /**\n   * Monitor resource loading performance\n   */\n  private monitorResourceLoading(): void {\n    new PerformanceObserver((list) => {\n      for (const entry of list.getEntries()) {\n        const resource = entry as PerformanceResourceTiming;\n        \n        // Monitor large bundle loads\n        if (resource.name.includes('.js') && resource.transferSize > 100000) {\n          this.recordMetric({\n            name: 'LargeBundle',\n            value: resource.responseEnd - resource.startTime,\n            timestamp: Date.now(),\n            url: resource.name,\n            userAgent: navigator.userAgent,\n            isCrisisUser: this.isCrisisUser()\n          });\n        }\n      }\n    }).observe({ entryTypes: ['resource'] });\n  }\n  \n  /**\n   * Record performance metric and check budgets\n   */\n  private recordMetric(metric: PerformanceMetric): void {\n    this.metrics.push(metric);\n    \n    // Limit metrics array size to prevent memory bloat\n    if (this.metrics.length > 1000) {\n      this.metrics = this.metrics.slice(-500);\n    }\n    \n    // Check performance budgets\n    this.checkPerformanceBudgets(metric);\n    \n    // Log crisis performance issues immediately\n    if (metric.isCrisisUser) {\n      this.checkCrisisPerformance(metric);\n    }\n  }\n  \n  /**\n   * Check performance budgets and trigger alerts\n   */\n  private checkPerformanceBudgets(metric: PerformanceMetric): void {\n    const budget = this.budgets.find(b => b.metric === metric.name);\n    if (!budget) return;\n    \n    const targetBudget = metric.isCrisisUser ? (budget.crisisBudget || budget.budget) : budget.budget;\n    const exceedancePercentage = ((metric.value - targetBudget) / targetBudget) * 100;\n    \n    if (exceedancePercentage > budget.threshold) {\n      this.triggerAlert({\n        type: 'budget_exceeded',\n        severity: metric.isCrisisUser ? 'emergency' : exceedancePercentage > 50 ? 'critical' : 'warning',\n        message: `Performance budget exceeded: ${metric.name} took ${metric.value.toFixed(2)}ms (budget: ${targetBudget}ms)`,\n        metrics: [metric]\n      });\n    }\n  }\n  \n  /**\n   * Special monitoring for crisis users\n   */\n  private checkCrisisPerformance(metric: PerformanceMetric): void {\n    const crisisThresholds = {\n      LCP: 1200,\n      FID: 50,\n      CLS: 0.05,\n      TTFB: 400,\n      API_Response: 1000\n    };\n    \n    const threshold = crisisThresholds[metric.name as keyof typeof crisisThresholds];\n    \n    if (threshold && metric.value > threshold) {\n      console.error(`[Crisis Performance] ${metric.name} exceeded crisis threshold: ${metric.value} > ${threshold}`);\n      \n      this.triggerAlert({\n        type: 'crisis_performance',\n        severity: 'emergency',\n        message: `CRISIS USER PERFORMANCE ISSUE: ${metric.name} = ${metric.value.toFixed(2)}ms (threshold: ${threshold}ms)`,\n        metrics: [metric]\n      });\n      \n      // Consider triggering rollback for critical crisis performance issues\n      if (metric.value > threshold * 2) {\n        this.considerRollback(`Crisis performance critical: ${metric.name}`);\n      }\n    }\n  }\n  \n  /**\n   * Monitor memory usage and detect leaks\n   */\n  private startMemoryMonitoring(): void {\n    if (typeof window === 'undefined' || !('performance' in window) || !('memory' in (window.performance as any))) {\n      return;\n    }\n    \n    let lastMemoryUsage = 0;\n    let memoryIncreaseCount = 0;\n    \n    setInterval(() => {\n      const memory = (window.performance as any).memory;\n      const currentUsage = memory.usedJSHeapSize;\n      \n      // Track memory growth\n      if (currentUsage > lastMemoryUsage * 1.1) {\n        memoryIncreaseCount++;\n      } else {\n        memoryIncreaseCount = 0;\n      }\n      \n      // Alert on potential memory leak\n      if (memoryIncreaseCount > 5 || currentUsage > 100 * 1024 * 1024) {\n        this.triggerAlert({\n          type: 'memory_leak',\n          severity: 'critical',\n          message: `Potential memory leak detected: ${(currentUsage / 1024 / 1024).toFixed(2)}MB used`,\n          metrics: [{\n            name: 'MemoryUsage',\n            value: currentUsage,\n            timestamp: Date.now(),\n            url: window.location.href,\n            userAgent: navigator.userAgent\n          }]\n        });\n      }\n      \n      lastMemoryUsage = currentUsage;\n    }, 30000); // Check every 30 seconds\n  }\n  \n  /**\n   * Trigger performance alert\n   */\n  private triggerAlert(alertData: Omit<PerformanceAlert, 'id' | 'timestamp'>): void {\n    const alert: PerformanceAlert = {\n      id: `perf_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,\n      timestamp: Date.now(),\n      ...alertData\n    };\n    \n    this.alerts.push(alert);\n    \n    // Limit alerts array size\n    if (this.alerts.length > 100) {\n      this.alerts = this.alerts.slice(-50);\n    }\n    \n    console.warn('[Performance Alert]', alert.message);\n    \n    // Send to monitoring service\n    this.sendToMonitoringService(alert);\n  }\n  \n  /**\n   * Consider triggering rollback\n   */\n  private async considerRollback(reason: string): Promise<void> {\n    console.error(`[Performance] Rollback consideration: ${reason}`);\n    \n    // Check if multiple rollback triggers are happening\n    const recentAlerts = this.alerts.filter(a => \n      Date.now() - a.timestamp < 60000 && // Last minute\n      (a.type === 'crisis_performance' || a.severity === 'emergency')\n    );\n    \n    if (recentAlerts.length >= 3) {\n      console.error('[Performance] Multiple critical issues detected, triggering rollback');\n      \n      for (const callback of this.rollbackCallbacks) {\n        try {\n          await callback();\n        } catch (error) {\n          console.error('[Performance] Rollback callback failed:', error);\n        }\n      }\n      \n      // Mark alerts as rollback triggered\n      recentAlerts.forEach(alert => {\n        alert.rollbackTriggered = true;\n      });\n    }\n  }\n  \n  /**\n   * Check if current user is in crisis mode\n   */\n  private isCrisisUser(): boolean {\n    return window.location.search.includes('crisis=true') ||\n           document.body.classList.contains('crisis-mode') ||\n           sessionStorage.getItem('crisis_mode') === 'true';\n  }\n  \n  /**\n   * Initialize performance observer\n   */\n  private initializeObserver(): void {\n    if (typeof window === 'undefined' || !('PerformanceObserver' in window)) {\n      return;\n    }\n    \n    try {\n      this.observer = new PerformanceObserver((list) => {\n        // Handle general performance entries\n        for (const entry of list.getEntries()) {\n          if (entry.entryType === 'measure' && entry.duration > 100) {\n            this.recordMetric({\n              name: entry.name,\n              value: entry.duration,\n              timestamp: Date.now(),\n              url: window.location.href,\n              userAgent: navigator.userAgent,\n              isCrisisUser: this.isCrisisUser()\n            });\n          }\n        }\n      });\n      \n      this.observer.observe({ entryTypes: ['measure', 'mark'] });\n    } catch (error) {\n      console.error('[Performance] Failed to initialize PerformanceObserver:', error);\n    }\n  }\n  \n  /**\n   * Send alert to monitoring service\n   */\n  private async sendToMonitoringService(alert: PerformanceAlert): Promise<void> {\n    try {\n      // In production, send to your monitoring service\n      if (process.env.NODE_ENV === 'production') {\n        await fetch('/api/performance/alerts', {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify(alert)\n        });\n      }\n    } catch (error) {\n      console.error('[Performance] Failed to send alert to monitoring service:', error);\n    }\n  }\n  \n  /**\n   * Get performance statistics\n   */\n  getStats(): {\n    metrics: PerformanceMetric[];\n    alerts: PerformanceAlert[];\n    budgetCompliance: Array<{ metric: string; compliance: number }>;\n    crisisUserMetrics: PerformanceMetric[];\n  } {\n    const crisisUserMetrics = this.metrics.filter(m => m.isCrisisUser);\n    \n    const budgetCompliance = this.budgets.map(budget => {\n      const relevantMetrics = this.metrics.filter(m => m.name === budget.metric);\n      if (relevantMetrics.length === 0) return { metric: budget.metric, compliance: 100 };\n      \n      const compliantMetrics = relevantMetrics.filter(m => {\n        const targetBudget = m.isCrisisUser ? (budget.crisisBudget || budget.budget) : budget.budget;\n        return m.value <= targetBudget;\n      });\n      \n      return {\n        metric: budget.metric,\n        compliance: (compliantMetrics.length / relevantMetrics.length) * 100\n      };\n    });\n    \n    return {\n      metrics: this.metrics.slice(-100), // Last 100 metrics\n      alerts: this.alerts.slice(-20), // Last 20 alerts\n      budgetCompliance,\n      crisisUserMetrics\n    };\n  }\n  \n  /**\n   * Register cleanup listeners\n   */\n  private registerCleanupListeners(): void {\n    if (typeof window === 'undefined') return;\n    \n    window.addEventListener('beforeunload', () => {\n      this.stopMonitoring();\n    });\n    \n    document.addEventListener('visibilitychange', () => {\n      if (document.hidden) {\n        // Reduce monitoring when page is hidden\n        console.log('[Performance] Page hidden, reducing monitoring');\n      } else {\n        // Resume full monitoring when page is visible\n        console.log('[Performance] Page visible, resuming full monitoring');\n      }\n    });\n  }\n}\n\n// Singleton instance\nexport const performanceMonitor = new PerformanceMonitor();\n\n// Auto-start monitoring\nif (typeof window !== 'undefined') {\n  performanceMonitor.startMonitoring();\n  \n  // Register default rollback callback\n  performanceMonitor.registerRollbackCallback(async () => {\n    console.warn('[Performance] Default rollback triggered - reloading page');\n    window.location.reload();\n  });\n}