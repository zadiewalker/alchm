/**
 * ALCHM Performance Rollback System
 * Crisis-Critical: Automated rollback triggers for performance regressions
 * Life-Safety: Immediate rollback when crisis resource loading is affected
 */

interface RollbackTrigger {
  metric: string;
  threshold: number;
  crisisThreshold?: number; // Stricter threshold for crisis users
  consecutiveViolations: number;
  enabled: boolean;
  description: string;
}

interface RollbackAction {
  id: string;
  type: 'cache_clear' | 'component_disable' | 'route_redirect' | 'page_reload' | 'emergency_mode';
  description: string;
  execute: () => Promise<void>;
  rollback?: () => Promise<void>; // To undo the action if needed
}

interface RollbackEvent {
  id: string;
  triggeredBy: string;
  threshold: number;
  actualValue: number;
  isCrisisUser: boolean;
  timestamp: Date;
  actionsExecuted: string[];
  success: boolean;
  error?: string;
}

export class PerformanceRollbackSystem {
  private triggers: Map<string, RollbackTrigger> = new Map();
  private actions: Map<string, RollbackAction> = new Map();
  private violationCounts: Map<string, number> = new Map();
  private rollbackHistory: RollbackEvent[] = [];
  private isEnabled = true;
  private emergencyMode = false;
  
  // Performance thresholds that trigger rollbacks
  private readonly DEFAULT_TRIGGERS: RollbackTrigger[] = [
    {
      metric: 'LCP',
      threshold: 4000, // 4 seconds for regular users
      crisisThreshold: 1500, // 1.5 seconds for crisis users
      consecutiveViolations: 2,
      enabled: true,
      description: 'Largest Contentful Paint rollback trigger'
    },
    {
      metric: 'FID',
      threshold: 500, // 500ms for regular users
      crisisThreshold: 100, // 100ms for crisis users
      consecutiveViolations: 3,
      enabled: true,
      description: 'First Input Delay rollback trigger'
    },
    {
      metric: 'CLS',
      threshold: 0.5, // 0.5 for regular users
      crisisThreshold: 0.1, // 0.1 for crisis users
      consecutiveViolations: 2,
      enabled: true,
      description: 'Cumulative Layout Shift rollback trigger'
    },
    {
      metric: 'MemoryUsage',
      threshold: 100 * 1024 * 1024, // 100MB
      crisisThreshold: 50 * 1024 * 1024, // 50MB for crisis users
      consecutiveViolations: 1, // Immediate action for memory issues
      enabled: true,
      description: 'Memory usage rollback trigger'
    },
    {
      metric: 'CrisisResourceLoad',
      threshold: 2000, // 2 seconds max for crisis resources
      crisisThreshold: 1000, // 1 second for crisis users
      consecutiveViolations: 1, // Immediate action for crisis resources
      enabled: true,
      description: 'Crisis resource loading rollback trigger'
    },
    {
      metric: 'JavaScriptErrors',
      threshold: 5, // 5 errors per minute
      crisisThreshold: 1, // 1 error per minute for crisis users
      consecutiveViolations: 1,
      enabled: true,
      description: 'JavaScript error rate rollback trigger'
    }
  ];
  
  constructor() {
    this.initializeTriggers();
    this.initializeActions();
    this.startMonitoring();
  }
  
  /**
   * Initialize rollback triggers
   */
  private initializeTriggers(): void {
    this.DEFAULT_TRIGGERS.forEach(trigger => {
      this.triggers.set(trigger.metric, trigger);
      this.violationCounts.set(trigger.metric, 0);
    });
    
    console.log('[Rollback] Initialized performance rollback triggers for crisis safety');
  }
  
  /**
   * Initialize rollback actions
   */
  private initializeActions(): void {\n    const actions: RollbackAction[] = [\n      {\n        id: 'clear_all_caches',\n        type: 'cache_clear',\n        description: 'Clear all application caches',\n        execute: async () => {\n          // Clear service worker caches\n          if ('caches' in window) {\n            const cacheNames = await caches.keys();\n            await Promise.all(cacheNames.map(name => caches.delete(name)));\n          }\n          \n          // Clear local storage (except critical data)\n          const criticalKeys = ['user_session', 'crisis_mode', 'auth_token'];\n          const keysToRemove = [];\n          for (let i = 0; i < localStorage.length; i++) {\n            const key = localStorage.key(i);\n            if (key && !criticalKeys.includes(key)) {\n              keysToRemove.push(key);\n            }\n          }\n          keysToRemove.forEach(key => localStorage.removeItem(key));\n          \n          console.log('[Rollback] Cleared all non-critical caches');\n        }\n      },\n      {\n        id: 'disable_animations',\n        type: 'component_disable',\n        description: 'Disable all animations and transitions',\n        execute: async () => {\n          // Add CSS to disable animations\n          const style = document.createElement('style');\n          style.id = 'rollback-disable-animations';\n          style.textContent = `\n            *, *::before, *::after {\n              animation-duration: 0.01ms !important;\n              animation-iteration-count: 1 !important;\n              transition-duration: 0.01ms !important;\n              scroll-behavior: auto !important;\n            }\n          `;\n          document.head.appendChild(style);\n          \n          console.log('[Rollback] Disabled animations for performance');\n        },\n        rollback: async () => {\n          const style = document.getElementById('rollback-disable-animations');\n          if (style) {\n            style.remove();\n            console.log('[Rollback] Re-enabled animations');\n          }\n        }\n      },\n      {\n        id: 'enable_emergency_mode',\n        type: 'emergency_mode',\n        description: 'Enable emergency performance mode',\n        execute: async () => {\n          this.emergencyMode = true;\n          \n          // Add emergency mode styles\n          const style = document.createElement('style');\n          style.id = 'rollback-emergency-mode';\n          style.textContent = `\n            .emergency-mode {\n              /* Disable complex layouts */\n              .grid { display: block !important; }\n              .flex { display: block !important; }\n              \n              /* Simplify typography */\n              * { font-family: system-ui, sans-serif !important; }\n              \n              /* Remove complex effects */\n              * { box-shadow: none !important; backdrop-filter: none !important; }\n            }\n          `;\n          document.head.appendChild(style);\n          document.body.classList.add('emergency-mode');\n          \n          // Show emergency mode indicator\n          this.showEmergencyModeIndicator();\n          \n          console.log('[Rollback] Enabled emergency performance mode');\n        },\n        rollback: async () => {\n          this.emergencyMode = false;\n          document.body.classList.remove('emergency-mode');\n          \n          const style = document.getElementById('rollback-emergency-mode');\n          if (style) style.remove();\n          \n          const indicator = document.getElementById('emergency-mode-indicator');\n          if (indicator) indicator.remove();\n          \n          console.log('[Rollback] Disabled emergency performance mode');\n        }\n      },\n      {\n        id: 'force_garbage_collection',\n        type: 'cache_clear',\n        description: 'Force garbage collection if available',\n        execute: async () => {\n          // Force garbage collection if available (development/debugging)\n          if ('gc' in window && typeof (window as any).gc === 'function') {\n            (window as any).gc();\n            console.log('[Rollback] Forced garbage collection');\n          }\n          \n          // Clear large objects from memory\n          this.clearLargeObjects();\n        }\n      },\n      {\n        id: 'redirect_to_minimal_page',\n        type: 'route_redirect',\n        description: 'Redirect to minimal performance page',\n        execute: async () => {\n          // Create minimal crisis page if in crisis mode\n          if (this.isCrisisUser()) {\n            this.createMinimalCrisisPage();\n          } else {\n            // Redirect to a lighter version of the current page\n            const currentUrl = new URL(window.location.href);\n            currentUrl.searchParams.set('minimal', 'true');\n            window.location.href = currentUrl.toString();\n          }\n        }\n      },\n      {\n        id: 'emergency_page_reload',\n        type: 'page_reload',\n        description: 'Emergency page reload as last resort',\n        execute: async () => {\n          console.warn('[Rollback] Emergency page reload triggered');\n          \n          // Save critical state before reload\n          const criticalState = {\n            crisis_mode: sessionStorage.getItem('crisis_mode'),\n            current_page: window.location.pathname,\n            emergency_reload: Date.now()\n          };\n          sessionStorage.setItem('rollback_state', JSON.stringify(criticalState));\n          \n          // Reload after a brief delay\n          setTimeout(() => {\n            window.location.reload();\n          }, 1000);\n        }\n      }\n    ];\n    \n    actions.forEach(action => {\n      this.actions.set(action.id, action);\n    });\n    \n    console.log('[Rollback] Initialized rollback actions');\n  }\n  \n  /**\n   * Start performance monitoring for rollback triggers\n   */\n  private startMonitoring(): void {\n    // Monitor Web Vitals\n    if (typeof window !== 'undefined' && 'PerformanceObserver' in window) {\n      this.observeWebVitals();\n    }\n    \n    // Monitor memory usage\n    this.monitorMemoryUsage();\n    \n    // Monitor JavaScript errors\n    this.monitorJavaScriptErrors();\n    \n    // Monitor crisis resource loading\n    this.monitorCrisisResources();\n    \n    console.log('[Rollback] Started performance monitoring for rollback triggers');\n  }\n  \n  /**\n   * Observe Web Vitals for rollback triggers\n   */\n  private observeWebVitals(): void {\n    try {\n      // LCP Observer\n      new PerformanceObserver((list) => {\n        for (const entry of list.getEntries()) {\n          this.checkTrigger('LCP', entry.startTime);\n        }\n      }).observe({ entryTypes: ['largest-contentful-paint'] });\n      \n      // FID Observer\n      new PerformanceObserver((list) => {\n        for (const entry of list.getEntries()) {\n          const fid = (entry as any).processingStart - entry.startTime;\n          this.checkTrigger('FID', fid);\n        }\n      }).observe({ entryTypes: ['first-input'] });\n      \n      // CLS Observer\n      new PerformanceObserver((list) => {\n        let clsValue = 0;\n        for (const entry of list.getEntries()) {\n          if (!(entry as any).hadRecentInput) {\n            clsValue += (entry as any).value;\n          }\n        }\n        this.checkTrigger('CLS', clsValue);\n      }).observe({ entryTypes: ['layout-shift'] });\n      \n    } catch (error) {\n      console.error('[Rollback] Failed to initialize Web Vitals observers:', error);\n    }\n  }\n  \n  /**\n   * Monitor memory usage\n   */\n  private monitorMemoryUsage(): void {\n    setInterval(() => {\n      if (typeof window !== 'undefined' && 'performance' in window && 'memory' in (window.performance as any)) {\n        const memory = (window.performance as any).memory;\n        this.checkTrigger('MemoryUsage', memory.usedJSHeapSize);\n      }\n    }, 10000); // Check every 10 seconds\n  }\n  \n  /**\n   * Monitor JavaScript errors\n   */\n  private monitorJavaScriptErrors(): void {\n    let errorCount = 0;\n    const errorWindow = 60000; // 1 minute window\n    \n    window.addEventListener('error', () => {\n      errorCount++;\n      this.checkTrigger('JavaScriptErrors', errorCount);\n      \n      // Reset count after window\n      setTimeout(() => {\n        errorCount = Math.max(0, errorCount - 1);\n      }, errorWindow);\n    });\n    \n    window.addEventListener('unhandledrejection', () => {\n      errorCount++;\n      this.checkTrigger('JavaScriptErrors', errorCount);\n      \n      setTimeout(() => {\n        errorCount = Math.max(0, errorCount - 1);\n      }, errorWindow);\n    });\n  }\n  \n  /**\n   * Monitor crisis resource loading\n   */\n  private monitorCrisisResources(): void {\n    // Override fetch to monitor crisis resource requests\n    const originalFetch = window.fetch;\n    \n    window.fetch = async (...args) => {\n      const url = args[0]?.toString() || '';\n      \n      // Check if this is a crisis-related resource\n      if (this.isCrisisResource(url)) {\n        const startTime = performance.now();\n        \n        try {\n          const response = await originalFetch(...args);\n          const loadTime = performance.now() - startTime;\n          \n          this.checkTrigger('CrisisResourceLoad', loadTime);\n          \n          return response;\n        } catch (error) {\n          // Crisis resource failed to load - immediate rollback\n          this.checkTrigger('CrisisResourceLoad', 10000); // Very high value to trigger rollback\n          throw error;\n        }\n      }\n      \n      return originalFetch(...args);\n    };\n  }\n  \n  /**\n   * Check if a metric violates rollback triggers\n   */\n  private checkTrigger(metric: string, value: number): void {\n    if (!this.isEnabled) return;\n    \n    const trigger = this.triggers.get(metric);\n    if (!trigger || !trigger.enabled) return;\n    \n    const isCrisisUser = this.isCrisisUser();\n    const threshold = isCrisisUser && trigger.crisisThreshold ? trigger.crisisThreshold : trigger.threshold;\n    \n    if (value > threshold) {\n      // Increment violation count\n      const currentCount = this.violationCounts.get(metric) || 0;\n      const newCount = currentCount + 1;\n      this.violationCounts.set(metric, newCount);\n      \n      console.warn(`[Rollback] Performance violation detected: ${metric} = ${value} > ${threshold} (${newCount}/${trigger.consecutiveViolations})`);\n      \n      // Check if we've reached the consecutive violation threshold\n      if (newCount >= trigger.consecutiveViolations) {\n        this.executeRollback(metric, threshold, value, isCrisisUser);\n        // Reset violation count after rollback\n        this.violationCounts.set(metric, 0);\n      }\n    } else {\n      // Reset violation count on successful metric\n      this.violationCounts.set(metric, 0);\n    }\n  }\n  \n  /**\n   * Execute rollback actions\n   */\n  private async executeRollback(\n    triggeredBy: string,\n    threshold: number,\n    actualValue: number,\n    isCrisisUser: boolean\n  ): Promise<void> {\n    const rollbackEvent: RollbackEvent = {\n      id: `rollback_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,\n      triggeredBy,\n      threshold,\n      actualValue,\n      isCrisisUser,\n      timestamp: new Date(),\n      actionsExecuted: [],\n      success: false\n    };\n    \n    try {\n      console.error(`[Rollback] EXECUTING PERFORMANCE ROLLBACK: ${triggeredBy} = ${actualValue} > ${threshold}`);\n      \n      // Determine rollback strategy based on trigger and user type\n      const rollbackStrategy = this.getRollbackStrategy(triggeredBy, isCrisisUser);\n      \n      // Execute rollback actions in order\n      for (const actionId of rollbackStrategy) {\n        const action = this.actions.get(actionId);\n        if (action) {\n          try {\n            await action.execute();\n            rollbackEvent.actionsExecuted.push(actionId);\n            console.log(`[Rollback] Executed action: ${action.description}`);\n          } catch (error) {\n            console.error(`[Rollback] Failed to execute action ${actionId}:`, error);\n          }\n        }\n      }\n      \n      rollbackEvent.success = true;\n      \n      // Send rollback notification\n      this.notifyRollback(rollbackEvent);\n      \n    } catch (error) {\n      rollbackEvent.error = error instanceof Error ? error.message : 'Unknown error';\n      console.error('[Rollback] Rollback execution failed:', error);\n    } finally {\n      this.rollbackHistory.push(rollbackEvent);\n      \n      // Keep only last 20 rollback events\n      if (this.rollbackHistory.length > 20) {\n        this.rollbackHistory = this.rollbackHistory.slice(-20);\n      }\n    }\n  }\n  \n  /**\n   * Get rollback strategy based on trigger and user type\n   */\n  private getRollbackStrategy(trigger: string, isCrisisUser: boolean): string[] {\n    // Crisis users get more aggressive rollback strategies\n    if (isCrisisUser) {\n      switch (trigger) {\n        case 'CrisisResourceLoad':\n          return ['clear_all_caches', 'disable_animations', 'redirect_to_minimal_page'];\n        case 'LCP':\n        case 'FID':\n          return ['clear_all_caches', 'disable_animations', 'enable_emergency_mode'];\n        case 'MemoryUsage':\n          return ['force_garbage_collection', 'clear_all_caches', 'emergency_page_reload'];\n        case 'JavaScriptErrors':\n          return ['clear_all_caches', 'emergency_page_reload'];\n        default:\n          return ['clear_all_caches', 'disable_animations'];\n      }\n    } else {\n      // Regular users get less aggressive strategies\n      switch (trigger) {\n        case 'LCP':\n        case 'FID':\n          return ['clear_all_caches', 'disable_animations'];\n        case 'MemoryUsage':\n          return ['force_garbage_collection', 'clear_all_caches'];\n        case 'JavaScriptErrors':\n          return ['clear_all_caches'];\n        default:\n          return ['clear_all_caches'];\n      }\n    }\n  }\n  \n  /**\n   * Create minimal crisis page\n   */\n  private createMinimalCrisisPage(): void {\n    document.body.innerHTML = `\n      <div style=\"\n        font-family: system-ui, sans-serif;\n        max-width: 600px;\n        margin: 50px auto;\n        padding: 20px;\n        line-height: 1.6;\n        background: #f9f9f9;\n        border-radius: 8px;\n      \">\n        <h1 style=\"color: #2d3748; margin-bottom: 20px;\">🛡️ Emergency Performance Mode</h1>\n        \n        <p style=\"color: #4a5568; margin-bottom: 20px;\">\n          ALCHM has activated emergency performance mode to ensure crisis resources remain accessible.\n          We've temporarily simplified the interface for optimal performance.\n        </p>\n        \n        <div style=\"\n          background: #fed7d7;\n          border-left: 4px solid #f56565;\n          padding: 15px;\n          margin: 20px 0;\n          border-radius: 4px;\n        \">\n          <h3 style=\"color: #c53030; margin: 0 0 10px 0;\">Crisis Resources Available 24/7</h3>\n          <ul style=\"color: #742a2a; margin: 0; padding-left: 20px;\">\n            <li><strong>National Suicide Prevention Lifeline:</strong> 988</li>\n            <li><strong>Crisis Text Line:</strong> Text HOME to 741741</li>\n            <li><strong>National Domestic Violence Hotline:</strong> 1-800-799-7233</li>\n            <li><strong>LGBT National Hotline:</strong> 1-888-843-4564</li>\n          </ul>\n        </div>\n        \n        <button onclick=\"window.location.reload()\" style=\"\n          background: #4299e1;\n          color: white;\n          border: none;\n          padding: 12px 24px;\n          border-radius: 6px;\n          cursor: pointer;\n          font-size: 16px;\n          margin-right: 10px;\n        \">Try Reloading</button>\n        \n        <button onclick=\"window.location.href='/crisis'\" style=\"\n          background: #48bb78;\n          color: white;\n          border: none;\n          padding: 12px 24px;\n          border-radius: 6px;\n          cursor: pointer;\n          font-size: 16px;\n        \">Crisis Resources</button>\n      </div>\n    `;\n  }\n  \n  /**\n   * Show emergency mode indicator\n   */\n  private showEmergencyModeIndicator(): void {\n    const indicator = document.createElement('div');\n    indicator.id = 'emergency-mode-indicator';\n    indicator.innerHTML = `\n      <div style=\"\n        position: fixed;\n        top: 0;\n        left: 0;\n        right: 0;\n        background: #fed7d7;\n        color: #c53030;\n        text-align: center;\n        padding: 8px;\n        font-weight: bold;\n        z-index: 10000;\n        font-size: 14px;\n      \">\n        ⚡ Emergency Performance Mode Active - Interface Simplified for Speed\n      </div>\n    `;\n    document.body.appendChild(indicator);\n    \n    // Add padding to prevent content overlap\n    document.body.style.paddingTop = '40px';\n  }\n  \n  /**\n   * Clear large objects from memory\n   */\n  private clearLargeObjects(): void {\n    // Clear large global objects that might be consuming memory\n    const globalsToClear = ['__REACT_DEVTOOLS_GLOBAL_HOOK__', '__NEXT_DATA__'];\n    \n    globalsToClear.forEach(global => {\n      if ((window as any)[global]) {\n        try {\n          delete (window as any)[global];\n          console.log(`[Rollback] Cleared global object: ${global}`);\n        } catch (error) {\n          console.warn(`[Rollback] Failed to clear global: ${global}`, error);\n        }\n      }\n    });\n  }\n  \n  /**\n   * Check if current user is in crisis mode\n   */\n  private isCrisisUser(): boolean {\n    return window.location.search.includes('crisis=true') ||\n           sessionStorage.getItem('crisis_mode') === 'true' ||\n           document.body.classList.contains('crisis-mode');\n  }\n  \n  /**\n   * Check if URL is a crisis resource\n   */\n  private isCrisisResource(url: string): boolean {\n    const crisisPatterns = [\n      '/crisis',\n      '/emergency',\n      '/support',\n      'crisis-detection',\n      'crisis-resource',\n      'hotline'\n    ];\n    \n    return crisisPatterns.some(pattern => url.includes(pattern));\n  }\n  \n  /**\n   * Notify about rollback execution\n   */\n  private notifyRollback(event: RollbackEvent): void {\n    console.error('[Rollback] Performance rollback executed:', {\n      trigger: event.triggeredBy,\n      threshold: event.threshold,\n      actual: event.actualValue,\n      crisis: event.isCrisisUser,\n      actions: event.actionsExecuted\n    });\n    \n    // Send to monitoring service\n    if (typeof fetch !== 'undefined') {\n      fetch('/api/monitoring/rollback', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify(event)\n      }).catch(error => {\n        console.error('[Rollback] Failed to report rollback event:', error);\n      });\n    }\n  }\n  \n  /**\n   * Get rollback system status\n   */\n  public getStatus(): {\n    enabled: boolean;\n    emergencyMode: boolean;\n    triggers: Array<{ metric: string; enabled: boolean; violations: number }>;\n    recentRollbacks: RollbackEvent[];\n  } {\n    return {\n      enabled: this.isEnabled,\n      emergencyMode: this.emergencyMode,\n      triggers: Array.from(this.triggers.entries()).map(([metric, trigger]) => ({\n        metric,\n        enabled: trigger.enabled,\n        violations: this.violationCounts.get(metric) || 0\n      })),\n      recentRollbacks: this.rollbackHistory.slice(-5)\n    };\n  }\n  \n  /**\n   * Manually trigger rollback (for testing)\n   */\n  public manualRollback(trigger: string, reason: string = 'Manual trigger'): void {\n    console.warn(`[Rollback] Manual rollback triggered: ${reason}`);\n    const isCrisisUser = this.isCrisisUser();\n    this.executeRollback(trigger, 0, 0, isCrisisUser);\n  }\n  \n  /**\n   * Disable rollback system\n   */\n  public disable(): void {\n    this.isEnabled = false;\n    console.warn('[Rollback] Performance rollback system disabled');\n  }\n  \n  /**\n   * Enable rollback system\n   */\n  public enable(): void {\n    this.isEnabled = true;\n    console.log('[Rollback] Performance rollback system enabled');\n  }\n}\n\n// Singleton instance\nexport const performanceRollbackSystem = new PerformanceRollbackSystem();\n\n// Register cleanup on page unload\nif (typeof window !== 'undefined') {\n  window.addEventListener('beforeunload', () => {\n    // Save rollback state for analysis\n    const status = performanceRollbackSystem.getStatus();\n    sessionStorage.setItem('rollback_final_state', JSON.stringify(status));\n  });\n  \n  // Global access for debugging\n  (window as any).performanceRollback = performanceRollbackSystem;\n}\n\n// Crisis safety validation\nconsole.log('[Rollback] Performance rollback system initialized - protecting crisis users from performance issues');