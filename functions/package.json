{
  "name": "alchm-functions",
  "description": "Firebase Functions for ALCHM trauma-informed journaling platform",
  "version": "1.0.0",
  "scripts": {
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint --ext .js,.ts src",
    "lint:trust-critical": "ESLINT_USE_FLAT_CONFIG=false eslint --ext .ts src/kheperaGateway.ts src/kheperaGatewayCore.ts src/containerTransitions.ts src/containerTransitionsCore.ts src/continuityRuntimeGateCore.ts src/runtimeAttestationVerifierCore.ts src/privacyService.ts src/dataRetentionService.ts src/auth.ts src/adminAuth.ts src/index.ts",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/functions/src/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.1",
    "openai": "^4.28.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@typescript-eslint/eslint-plugin": "^6.19.1",
    "@typescript-eslint/parser": "^6.19.1",
    "eslint": "^8.56.0",
    "eslint-plugin-import": "^2.29.1"
  },
  "private": true
}
